apply plugin: 'java'
apply plugin: 'eclipse'


def prob_version = "2.0.0-milestone-14-SNAPSHOT"
def mainClass = "HelloProB"

repositories {
	maven {
		name "cobra"
		url "http://cobra.cs.uni-duesseldorf.de/artifactory/repo"
	}
}

dependencies {
	compile "de.prob:de.prob.core.kernel:${prob_version}"
}

task run(type:JavaExec) {
   main = mainClass
   args = ["-s", "-local"]
   classpath = sourceSets.main.runtimeClasspath
}
